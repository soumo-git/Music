<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    
    <!-- Internet for YouTube API and downloads -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- Audio effects -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    
    <!-- WiFi Direct for Duo feature -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        android:minSdkVersion="33"/>
    
    <!-- Vibration for notifications -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    
    <!-- Audio recording for voice messages -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Music">

        <!-- Splash Activity -->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Music.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Music"
            android:windowSoftInputMode="adjustPan"/>

        <!-- Songs List Activity -->
        <activity
            android:name=".ui.activity.SongsListActivity"
            android:exported="false"
            android:theme="@style/Theme.Music"
            android:parentActivityName=".ui.activity.MainActivity"/>

        <!-- Duo Songs List Activity -->
        <activity
            android:name=".duo.ui.activity.DuoSongsListActivity"
            android:exported="false"
            android:theme="@style/Theme.Music"
            android:parentActivityName=".ui.activity.MainActivity"/>

        <!-- Settings Activity -->
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Music"
            android:parentActivityName=".ui.activity.MainActivity"/>

        <!-- Equalizer Activity -->
        <activity
            android:name=".equalizer.ui.EqualizerActivity"
            android:exported="false"
            android:theme="@style/Theme.Music"
            android:parentActivityName=".ui.activity.MainActivity"/>

        <!-- Video Player Activity -->
        <activity
            android:name=".videoplayer.ui.VideoPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.Music.VideoPlayer"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".ui.activity.MainActivity"/>

        <!-- Legal Document Activity -->
        <activity
            android:name=".legal.LegalDocumentActivity"
            android:exported="false"
            android:theme="@style/Theme.Music"
            android:parentActivityName=".ui.activity.MainActivity"/>

        <!-- Music Service -->
        <service
            android:name=".service.MusicService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>

        <!-- Download Service -->
        <service
            android:name=".download.service.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Music Widget -->
        <receiver
            android:name=".widget.MusicWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="com.android.music.widget.PLAY_PAUSE"/>
                <action android:name="com.android.music.widget.NEXT"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info"/>
        </receiver>

    </application>

</manifest>
